use crate::{error::Result, input::{InputController, Point}};

impl InputController {
	pub fn click(&self, point: Point) -> Result<()> {
		todo!()
	}

    pub fn human_click(&self, point: Point) -> Result<()> {
        todo!()
    }
}
